import java.time.LocalDate;
import java.util.List;
import java.util.function.BiFunction;
import java.util.function.Function;
import java.util.stream.Collectors;

public class Project {
	
	//public static final  Function<List<Employee>,Integer>computeBonus=
			
//			list->list.stream().map(x->x.getHireDate()).
//			flatMapToDouble(x->400*( LocalDate.now().getYear()-x.getYear())).;
//			
		
			
          //  x -> 400*( LocalDate.now().getYear()-x.getHireDate().getYear());
	

}